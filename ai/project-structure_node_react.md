# Project Structure

Backend: NestJS
Frontend: React

backend/
   src/
      modules/
      controllers/
      services/
frontend/
   src/
      components/
      pages/

ai/
ai-context/

Important Notes:

- NestJS backend APIs are inside `backend/src`.
- React frontend is inside `frontend/src`.
- AI prompts are stored in `/ai`.
- Module documentation is stored in `/ai-context`.